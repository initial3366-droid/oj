# Generate with: openssl rand -hex 32
GO_JUDGE_AUTH_TOKEN=replace-with-a-long-random-token
GO_JUDGE_PORT=15050
# A 2 GiB container safely supports one 1 GiB submission at a time.
GO_JUDGE_PARALLELISM=1
